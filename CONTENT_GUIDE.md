# 内容格式

每篇内容都需要 YAML frontmatter。课程的 `slug` 是其他课程和培养方案引用它时使用的稳定 ID，修改标题时不要随意修改 slug。

课程关系字段：

- `prerequisites`: 建议先学习的课程 slug
- `nextCourses`: 以该课程为基础的后续课程 slug

`sourceStatus` 可选：`official`、`experience`、`unverified`。

涉及政策、考试、学分或时间安排时，正文必须提醒读者以学校和学院最新通知为准。
